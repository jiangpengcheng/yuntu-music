# 开发约定

- 保持 Java 7 语法和 Android API 19 兼容性；较新 API 使用已隔离的版本检查/反射路径。
- 网络、加密和播放器准备不能阻塞主线程；保持现有超时、资源释放和重试边界。
- 不添加绕过证书校验的网络逻辑，不重放喜欢/收藏等写请求，不在日志记录会话凭证。
- Java 使用 google-java-format 的标准格式；Python 使用四空格、明确的函数与入口。
- UI 修复验证对应的交互场景，涉及协议/网络/发布边界的改动补充回归测试。
- 运行 README 中的主机和模拟器测试。PR 中明确模拟器、API 19 编译和实车验证的区别。
- 不提交 APK、构建输出、真实账号信息或签名材料。公共协议常量和合成测试向量可提交，但应明确用途。
- 功能更新无需修改版本；发布时统一修改 Manifest 和 release notes。
